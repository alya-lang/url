# Types and data structures for url
# Part of the Alya Language package ecosystem (https://github.com/alya-lang)

struct UrlConfig
    name
    prefix
    count
end

function make_config(name = "World", count = 1)
    return UrlConfig {
        name: name,
        prefix: "Hello",
        count: count
    }
end
